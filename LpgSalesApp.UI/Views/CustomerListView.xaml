<UserControl x:Class="LpgSalesApp.UI.Views.CustomerListView"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:vm="clr-namespace:LpgSalesApp.UI.ViewModels"
     xmlns:local="clr-namespace:LpgSalesApp.UI.Converters"
     Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <local:NullToBoolConverter x:Key="NullToBoolConverter" />
    </UserControl.Resources>
    
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Refresh" Width="80" Margin="0 0 5 0" Command="{Binding RefreshCommand}" />
            <Button Content="Add" Width="80" Command="{Binding AddCommand}" />
            <Button Content="Edit" Width="80" Margin="0 0 5 0" Command="{Binding EditCommand}" IsEnabled="{Binding SelectedCustomer, Converter={StaticResource NullToBoolConverter}}"/>
            <Button Content="Delete" Width="80" Command="{Binding DeleteCommand}" IsEnabled="{Binding SelectedCustomer, Converter={StaticResource NullToBoolConverter}}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding ContactNumber}" Width="150"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
    
</UserControl>
    
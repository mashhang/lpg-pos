<Window x:Class="LpgSalesApp.UI.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="600" WindowStartupLocation="CenterScreen">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="EDREL GAS" FontSize="44" FontWeight="Bold" FontFamily="Cambria" Foreground="#266625" Margin="0 0 0 10" />

        <!-- Form Inputs -->
        <StackPanel Grid.Row="1" Margin="0 20 0 0">
            <TextBlock Text="👤 Username" Foreground="#484848" FontWeight="SemiBold" Margin="0 5 0 0"/>
            <Border CornerRadius="8" BorderBrush="#266625" BorderThickness="1" Margin="0 5 0 10">
                <TextBox Text="{Binding Username}" Margin="5" BorderThickness="0" FontSize="24"/>
            </Border>

            <TextBlock Text="🔒 Password" Foreground="#484848" FontWeight="SemiBold" Margin="0 5 0 0"/>
            <Border CornerRadius="8" BorderBrush="#266625" BorderThickness="1" Margin="0 5 0 10">
                <PasswordBox Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" BorderThickness="0" Margin="5" FontSize="24" />
            </Border>
        </StackPanel>

        <!-- Login Button -->
        <Border Grid.Row="2" CornerRadius="8" BorderBrush="#266625" BorderThickness="3" Width="120" Margin="0 10 0 0">
            <Button Content="🔐 Login"
            Width="100"
            Height="40"
            Background="White"
            Foreground="#266625"
            FontWeight="Bold"
            BorderThickness="0"
            HorizontalAlignment="Center"
            Command="{Binding LoginCommand}" />
        </Border>

    </Grid>
</Window>

<UserControl x:Class="LpgSalesApp.UI.Views.TransactionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LpgSalesApp.UI.Converters"
             xmlns:vm="clr-namespace:LpgSalesApp.UI.ViewModels"
             Loaded="UserControl_Loaded">

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Refresh" Width="80" Margin="0 0 5 0" Command="{Binding RefreshCommand}" />
            <Button Content="New Transaction" Width="150" Command="{Binding AddCommand}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="200"/>
                <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate}" Width="150"/>
                <DataGridTextColumn Header="Item" Binding="{Binding ProductSummary}" Width="150"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat='₱{0:N2}'}" Width="100"/>
                <DataGridTextColumn Header="CreatedBy" Binding="{Binding CreatedBy}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
